---
import Common from '@pages/_common.astro'
import LoginCard from './login'

const { path } = Astro.params
const session = Astro.locals.session
console.log(session)
---

<Common title={path}>
	<a href="/api/login/github">Sign in with GitHub</a>
	{
		session ? (
			<>
				<h1>logged</h1>
				{Object.entries(session).map(([k, v]) => (
					<p>
						`${k}: ${v}`
					</p>
				))}
			</>
		) : (
			<LoginCard />
		)
	}
</Common>
